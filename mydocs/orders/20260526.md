<<<<<<< HEAD
# 2026-05-26 — 오늘 할일

## PR #1101 처리 — 글자겹침(hp:compose) 동그라미 글자 누락 + 한컴2024 시각 정합

- [x] PR #1101 정보 수집 (`gh pr view`) — 컨트리뷰터 HaimLee-4869 (8번째 PR)
- [x] 컨트리뷰터 사이클 점검 — 직전 #1088 시각 회귀 거절 사례 인지
- [x] cherry-pick + 자동 검증 (build / fmt / clippy --lib / test / WASM)
- [x] **별도 발견 — CanvasKit charOverlap 미지원** (rhwp-studio 영역 13페이지 사각형 미출력) → 이슈 #1126 등록
- [x] 시각 검증 — k-water-rfp.hwpx 13페이지 SVG 영역 (3개 반전 사각형 정상 출력 + font-size 0.80 가설 정합)
- [x] review 문서 작성 — `mydocs/pr/pr_1101_review.md` + 작업지시자 MERGE 승인
- [x] `gh pr merge 1101 --merge --admin` — merge commit `3ad7f75f`
- [x] local/devel ff 동기 (devel ← origin/devel) + review 브랜치 (`local/pr1101-review`, `pr-1101`) 정리
- [x] final report 작성 — `mydocs/pr/pr_1101_report.md`
- [x] 오늘할일 문서 갱신 (본 파일)

## PR #1102 처리 — 회전 90°/270° 이미지 bbox 이중회전 정정

- [x] PR #1102 정보 수집 — 동일 컨트리뷰터 HaimLee-4869 (9번째 PR)
- [x] cherry-pick + 자동 검증 (build / fmt / clippy --lib / test) 모두 통과
- [x] 관련 영역 검토 — `ShapeTransform::effective_image_bbox` 신규 + 3개 backend 적용
- [x] **결함 소견** — `src/paint/builder.rs:86` 영역 영역 동일 정정 누락 (rhwp-studio CanvasKit 영역 결함 잔존 가능성)
- [x] 별도 후속 이슈 #1127 등록 — `feedback_image_renderer_paths_separate` 4 path sweep 영역 영역 1개 누락
- [x] review 문서 — `mydocs/pr/pr_1102_review.md` + 작업지시자 결함 소견 상세 보고 + MERGE 승인
- [x] `gh pr merge 1102 --merge --admin` — merge commit `4e2c938f`
- [x] local/devel ff 동기 + review 브랜치 정리
- [x] final report 작성 — `mydocs/pr/pr_1102_report.md`

## Task #1116 — PR #1120 CI Build & Test 실패 수정 (컨트리뷰터 jangster77 작업 일지, PR 머지 시 보존)

- 이슈: [edwardkim/rhwp#1116](https://github.com/edwardkim/rhwp/issues/1116)
- PR: [edwardkim/rhwp#1120](https://github.com/edwardkim/rhwp/pull/1120)
- 브랜치: `local/task1116`
- 상태: CI 실패 원인 수정, SVG 스냅샷 갱신 및 로컬 검증 완료

### 진행 항목

1. GitHub Actions `Build & Test` 실패 확인:
   - 실패 job: `https://github.com/edwardkim/rhwp/actions/runs/26406618699/job/77731349901?pr=1120`
   - 실패 단계: `Run tests`
   - 실패 테스트: `renderer::height_cursor::tests::page_path_sb_prededuct`
2. 로컬 재현:
   - `cargo test page_path_sb_prededuct --lib -- --nocapture` 실패 재현.
3. 원인 분류:
   - #1116에서 HWP3-origin sample16 p3 정합을 위해 `spacing_before` 사전 차감을 생략한 수정이 공용 `vpos_corrected_end_y()` 전체에 적용됨.
   - 일반 문서의 #643/#1027 기대 동작은 `spacing_before` 사전 차감을 유지해야 하므로 기존 테스트 실패가 타당함.
4. 수정:
   - `HeightCursor`에 `skip_spacing_before_prededuct` 플래그 추가.
   - 일반 경로는 `raw_end_y - curr_sb`를 유지.
   - HWP3-origin 흐름에서만 `raw_end_y`를 그대로 사용.
   - layout/typeset 양쪽에 동일 플래그 전달.
5. 보고:
   - `mydocs/working/task_m100_1116_stage21.md` 작성.
6. PR #1120 CI 재확인 중 추가 실패 처리:
   - `cargo test`가 `tests/svg_snapshot.rs`까지 진행한 뒤 7개 golden 불일치로 실패.
   - #1116의 SVG/browser 폭 고정 변경으로 `textLength`/`lengthAdjust` 속성과 일부 tab leader 끝 좌표가 달라진 것이므로 golden을 갱신.
   - `UPDATE_GOLDEN=1 cargo test --test svg_snapshot -- --nocapture`로 7개 golden SVG를 갱신.
   - 테스트가 만든 `.actual.svg` 임시 파일은 제거.

### 검증 기록

- `cargo test page_path_sb_prededuct --lib -- --nocapture` 통과.
- `cargo test hwp3_origin_page_path_keeps_spacing_before_in_vpos --lib -- --nocapture` 통과.
- `cargo test --lib` 통과: 1387 passed, 0 failed, 6 ignored.
- `cargo test --test issue_1116 -- --nocapture` 통과: 13 passed.
- `cargo test --test issue_1105 -- --nocapture` 통과: 14 passed.
- `cargo fmt --all -- --check` 통과.
- `git diff --check` 통과.
- `cargo build --bin rhwp` 통과.
- `cargo test --test svg_snapshot -- --nocapture` 통과: 8 passed.

## PR #1120 처리 — sample16 한컴 3mm 격자 정합 후속 (메인테이너 일지)

- [x] PR #1120 정보 수집 — 컨트리뷰터 jangster77 (15번째 PR, HWP3 핵심 작업자)
- [x] supersede 체인 점검 (#1118 close → #1119 close → #1120 OPEN)
- [x] 충돌 해결 (orders/20260526.md — 메인테이너 + 컨트리뷰터 양쪽 일지 보존)
- [x] 자동 검증 — build / fmt / clippy --lib / cargo test --tests (svg_snapshot 8 + tab_cross_run 1)
- [x] 핵심 설계 점검 — `hwp3_variant_flow_spacing_before` 분기, tab leader 정밀 처리, text_measurement, `skip_spacing_before_prededuct` 가드
- [x] 컨트리뷰터 메모리 룰 3개 신규 수용 (`feedback_pr_requires_explicit_approval/_body_korean_required/_ci_before_pr`)
- [x] review 문서 작성 — `mydocs/pr/pr_1120_review.md` + 작업지시자 MERGE 승인
- [x] 메인테이너 측 직접 머지 (BEHIND + CONFLICTING → admin merge 불가 → 로컬 충돌 해결 + push)
- [x] final report 작성 — `mydocs/pr/pr_1120_report.md`

## Task #1122 + #1124 — PR #1125 통합 작업 (컨트리뷰터 jangster77 작업 일지, PR 머지 시 보존)

| 항목 | 제목 | 상태 | 메모 |
|------|------|------|------|
| Task #1122 | 3-11월 실전 통합 2022 p4 수식 over+숫자 결합 및 문26 그림 표시 개선 | 최종 승인 대기 | 작업지시자 제보: `samples/3-11월_실전_통합_2022.hwp` page 4 수식 표현식과 문 26) 그림이 한컴오피스 2022와 다르게 표시됨. 작업지시자 확인에 따라 별도 이슈를 추가 등록하지 않고 #1122 범위에서 처리. `over`/`atop` 뒤 숫자 결합 토큰 분리와 picture current size 반영을 구현. `gh issue edit 1122 --add-assignee @me`는 현재 로그인 계정 권한 부족으로 실패. #1124와 통합 브랜치 `local/task1124-hwpx-column-line`에 포함했고 PR 전 CI 통과. 최종 PR 생성은 최종사용자 승인 후 진행. |
| Task #1124 | HWPX 다단 세로 구분선 렌더링 누락 개선 | 최종 승인 대기 | 작업지시자 제보: `samples/3-11월_실전_통합_2022.hwpx` page 4에서 한컴/HWP 렌더와 달리 좌우 단 사이 세로 구분선이 표시되지 않음. 열린 이슈 검색 결과 동일 증상 없음. 이슈 [#1124](https://github.com/edwardkim/rhwp/issues/1124) 등록. 브랜치 `local/task1124-hwpx-column-line`. HWPX 원문에는 `<hp:colLine type="SOLID" width="0.12 mm" color="#000000"/>`가 있으나 `src/parser/hwpx/section.rs::parse_col_pr()`가 `colPr` 속성만 읽고 자식 `colLine`을 파싱하지 않는 것이 원인. 승인 후 구현했고 HWPX page 4 SVG에 `x=396.853...` 단 구분선이 생성됨을 확인. #1122와 통합 PR 준비 완료, 최종 승인 대기. |
