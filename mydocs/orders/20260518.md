# 2026-05-18 — 오늘 할일

## Task #967 — HWP3 sample18.hwp 페이지 수 +2 inflate (PR 작업 중)

- [x] Issue 등록 + branch + 수행/구현 계획서 + 승인
- [x] Stage 1 — pi=27 / pi=164 (빈 paragraph) 직후 [쪽나누기] (pi=28/165) case 식별
  - typeset.rs 의 next_will_vpos_reset 가드의 next_force_break 제외 조건 (hwp-multi-001 회귀 차단) 이 본 case 못 catch
- [x] Stage 2 — Fix B 선정 (별도 분기로 빈 paragraph + 다음 쪽나누기 case 만 추가 catch)
- [x] Stage 3 — Fix 적용 + sample18 단위 검증 (69 → 67 ✓)
- [x] Stage 4 — cargo test 1288 + 16 sample page count 회귀 0 (hwp-multi-001 보존)
- [x] Stage 5 — 한컴 정합 + 최종 보고서

## 후속

- HWPX sample18-hwp5.hwpx +7 inflate (별도 issue)
- samples/hwp3-sample18.hwp fixture 별도 추가 권장 (회귀 가드 — 본 PR 미포함)

---

## M100 — PR 처리 (메인테이너, 5/18 사이클)

@jangster77 PR 시리즈 (5/17~18) — 연속 5 PR (#956~#964, 원 Issue #952 5 분리 결함) + 추가 #966/#968. 5/17 PR (#956~#966) 처리 기록은 `mydocs/orders/20260517.md` 의 "M100 — PR 처리" 섹션 참조.

| PR | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| **PR #968 (closes #967)** | fix — 빈 paragraph + 다음 [쪽나누기] case 단독 page 차단 (HWP3 sample18 +2 inflate 해소) | **완료 (옵션 A — 본질 commit cherry-pick + no-ff merge `d0c25754`, 작업지시자 시각 판정 통과)** | 컨트리뷰터 @jangster77 — **24+ 사이클** 핵심 컨트리뷰터 — **@jangster77 PR 시리즈 마지막** (연속 5 PR #956~#964 + #966 + 추가 #968). 본질: Issue #967 — HWP3 sample18 페이지 수 rhwp 69 vs 한컴 67 (+2 inflate). 빈 paragraph (pi=27/164) 직후 [쪽나누기] (pi=28/165) → 빈 paragraph 별도 page 분기 → 단독 빈 페이지. Root cause (typeset.rs:555-584): next_force_break (쪽나누기) 시 next_will_vpos_reset 가드 미발동 (hwp-multi-001 회귀 차단 목적) → 빈 paragraph 단독 page. 정정 (`src/renderer/typeset.rs` :585 별도 분기): next_force_break && is_curr_empty && empty_h_px > avail (overflow) 시에만 continue (skip → 단독 page 차단), fit 가능 시 정상 emit. **v2 정밀화**: v1 (조건 무관 skip) → aift.hwp snapshot 회귀 → v2 (overflow 한정) → aift.hwp 18 case 정상 emit + sample18 fix. 영역 좁힘: 빈 paragraph + 쪽나누기 + overflow 한정 — fit 가능/비-빈/일반 paragraph/hwp-multi-001 영향 없음. 본 환경 cherry-pick 충돌 0건 — typeset.rs auto-merge (devel Task #836 Endnote :1064/1091 보존 + PR #968 :585 분기 양립). orders/20260518.md 신규 (5/18, 컨트리뷰터 Task #967 작업 일지). devel merge commit 2개 cherry-pick 제외 — 본질 `3c1ea870` 만. task_m100_967* 8 신규 추가. 결정적 검증: cargo test --release 전체 (lib **1288 passed** + integration svg_snapshot 8) ALL GREEN + cargo clippy --release -D warnings 통과 + **광범위 sweep 7 fixture / 169 페이지 / 169 same / 0 diff** (aift.hwp 74 same — v1 회귀 해소 입증) + WASM 4.4 MB 재빌드. **⚠️ samples/hwp3-sample18.hwp 영역 영역 PR 미포함 + 본 환경 부재** (PR 본문 fixture 추가 명시 부재, git 전체 history 추가 commit 부재, pr-968-tmp branch 부재). 작업지시자 결정 영역 영역 sweep 169/169 same + cargo test 전체 통과 영역 영역 회귀 부재 입증 + sample18 페이지 수 69→67 정합 영역 영역 PR 본문 신뢰 영역 영역 머지. **작업지시자 시각 판정** ✅ 통과 (기존 다른 HWP 3.0 샘플 — sample10~16/19 등 — 회귀 부재 확인, sample18 자체 영역 영역 fixture 부재 영역 영역 다른 HWP3 샘플 + sweep 169/169 same 영역 영역 회귀 가드 보완). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff. Issue #967 close. 잔존: HWPX sample18-hwp5.hwpx +7 inflate (별도 task) + samples/hwp3-sample18.hwp fixture 별도 추가 권장 (회귀 가드). 검토 보고서: `mydocs/pr/archives/pr_968_review.md`. 처리 보고서: `mydocs/pr/archives/pr_968_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 24+ 사이클 (PR 시리즈 마지막). **`feedback_image_renderer_paths_separate` 정합** — typeset.rs pagination 단일. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화** — v1 (조건 무관 skip) → CI snapshot 회귀 → v2 (overflow 조건 한정) 일반화 위험 발견 후 케이스별 정밀화 입증. **`feedback_diagnosis_layer_attribution` 정합** — next_force_break 가드 미발동 (hwp-multi-001 회귀 차단 목적) root cause 정확 진단. **`feedback_visual_judgment_authority` 권위 사례 강화** — fixture (sample18) 부재 영역 영역 sweep 169/169 same + cargo test 전체 + 기존 다른 HWP3 샘플 시각 판정 영역 영역 회귀 가드 보완 (작업지시자 결정). **`reference_authoritative_hancom` 정합** — sample18.hwp 한컴 67 페이지 정합 기준. **`feedback_self_verification_not_hancom` 정합** — fixture 부재 영역 영역 PR 본문 자기 보고 (69→67) 신뢰 + 회귀 부재 입증 영역 영역 보완 (sample18 자체 한컴 검증 영역 영역 컨트리뷰터 환경 신뢰). |

---

## @jangster77 PR 시리즈 (5/17~18) 완결 총평

| PR | Issue | 본질 | 진단 도구 | 머지 |
|----|-------|------|----------|------|
| #956 | #952 Issue 1 | page border paper/body | bisect (`4bb11289`) | `b31e38ff` |
| #958 | #957 (Issue 2) | sample16 p18 빈 caption phantom | RHWP_DEBUG_TAC_CURSOR | `0b630773` |
| #961 | #959 (Issue 3) | 시험지 p1 문9 column picture advance | RHWP_DEBUG_TAC_CURSOR | `586e3cc0` |
| #963 | #960 (Issue 4) | 시험지 p2 cases off-by-one | RHWP_DEBUG_PARA_TAC | `415b9d8d` |
| #964 | #962 (Issue 5) | 시험지 p2 보기 textbox duplicate | SVG 분석 | `808f419e` |
| #966 | #965 | WMF SetTextAlign vertical bits | PR #918 Stage 33-A 포팅 | `235e049c` |
| #968 | #967 | HWP3 sample18 +2 inflate | next_force_break 가드 v2 | `d0c25754` |

- 7 PR 모두 옵션 A (cherry-pick + 자기 검증 + 작업지시자 시각 판정 + no-ff merge)
- 원 Issue #952 (1 통합 issue → 5 분리 결함) 완결 + WMF/HWP3 추가 2개
- 환경변수 진단 도구 (RHWP_DEBUG_*) 영구화 — 차후 재회귀 추적 인프라
- 정정 모두 다른 파일/영역 — 정확한 결함 분리 + 양립 (layout.rs ×3 + paragraph_layout.rs + shape_layout.rs + svg/mod.rs + typeset.rs)
- 매 PR cargo test 1288 + sweep 169 + 작업지시자 시각 판정 — 결정적 검증 일관
- PR #963 exam_math p18 / PR #968 sample18 fixture 부재 — 작업지시자 한컴 직접 확인 / 회귀 가드 보완 (`feedback_visual_judgment_authority` 권위)
- PR #918 (CLOSED, +5082 거대 PR) → #966 (root cause ~60 lines 단독 포팅) — 작은 단위 분리 입증

---

## v0.7.12 PATCH 릴리즈 (5/18, 메인테이너)

- 방식: 1 방식 + 패턴 A (v0.7.11 동일). 기준 v0.7.11 (`a9dcdee3`) → v0.7.12 (`1899ef9b`).
- 버전 4 파일 (Cargo.toml + rhwp-vscode/npm-editor/rhwp-studio package.json) + CHANGELOG 3종 (ko/en/vscode, [0.7.11] 소급 보강 포함).
- 흡수: 422 files / +64761 / -3328. @jangster77 7-PR (#956~#968) + PR #818 LTO + rhwp-studio (#799/#814/#811/#808/#809 등) + dependabot 7건.
- 자기 검증: cargo test 1288 + svg_snapshot 8 + tab_cross_run 1 ALL GREEN, WASM 4.6 MB 재빌드.
- `feedback_release_sync_check`: main 4 commit 전부 과거 릴리즈 PR merge commit, `git diff origin/devel...origin/main` 공집합 → 실제 분기 없음 확인 후 진행.
- 절차: local/devel 커밋 `a0947774` → devel no-ff `0ff97dec` push → **PR #970** 생성 → `--admin` merge (자기 PR 자기 승인 불가 → bypass, v0.7.11 패턴) → main `1899ef9b` 에 **v0.7.12 태그** push → local/devel ff 정렬.
- 보고서: `mydocs/report/task_m100_v0712_release_report.md`.
- 잔존: HWPX sample18-hwp5.hwpx +7 inflate (별도 issue), samples/hwp3-sample18.hwp fixture 추가 권장 (#968 회귀 가드).
