# 2026-05-22 — 오늘 할일

## Task M100-1058 reopen — 한컴 contract 4 영역 추가 정정 (closes #1058, 메인테이너)

- [x] 작업지시자 한컴 시각 판정 부적합 보고 (Stage 4 머지 후) — 글상자 없는 fixture (footnote-01.hwpx) 도 동일 다단계 목록 부작용 발생
- [x] 작업지시자 결정적 정보 수용:
  - 글상자 없는 케이스도 발생 → 글상자 LIST_HEADER 13 byte (Task #1058 1차) 가 본질 아님
  - HWP→HWP 라운드트립 정상 → HWPX→HWP 만의 본질
- [x] Stage 4-pivot "HWP IR oracle 방식" 재적용 — HWP 정답지 record vs HWPX→HWP 저장본 record-level 차이 분석
- [x] **Round 1 (Stage 5~9): PARA_HEADER instance_id**:
  - footnote-01.hwp 의 모든 PARA_HEADER (58개) 의 마지막 byte 0x80 vs rhwp 0x00 차이
  - HWPX `<hp:p id="2147483648">` (= 0x80000000) 매핑 누락
  - 정정: `src/parser/hwpx/section.rs::parse_paragraph` — raw_header_extra offset 6..10 작성
  - 작업지시자 한컴 시각 판정 통과: "문단번호 1.1.1.1.1.1 부여 사라짐"
- [x] **Round 2 (Stage 10~12): Style record lang_id**:
  - 작업지시자 보고: "왼쪽 여백 60.0pt + 줄간격 160% 자동 설정"
  - 작업지시자 통찰: "스타일 바인딩이 잘못된 것은 아닌가 확인 필요"
  - HWP5 spec 표 47 정합 분석 — Style record 의 INT16 lang_id + trailing UINT16 zero 4 byte 누락
  - 정정 (4 파일):
    * `src/model/style.rs` — Style.lang_id 필드 추가
    * `src/parser/doc_info.rs::parse_style` — lang_id 읽기 + trailing 흡수
    * `src/serializer/doc_info.rs::serialize_style` — lang_id + trailing zero 작성
    * `src/parser/hwpx/header.rs::parse_style` — HWPX langID 매핑
  - 추가: `src/parser/hwpx/header.rs::parse_para_shape` — line_spacing_v2 보정
  - 작업지시자 한컴 시각 판정 통과: "각주 삽입 시 정상 동작"
- [x] **Round 3 (Stage 13): ParaShape attr1 부작용 정정**:
  - 작업지시자 보고: "일반 문단 시작에 글머리표 처리"
  - 정답지 vs 저장본 attr1 비교 — ps[5] ORACLE 0x00000000 vs rhwp 0x00000080 (강제 set)
  - `ps.attr1 |= 0x80` (bit 7) 강제 set 제거 → HWPX 의 attr1 값 그대로 보존
  - 본질 미해결 (잔존 차이 발견 필요)
- [x] **Round 4 (Stage 14): HWPX `<hh:bullet>` 파싱**:
  - 작업지시자 통찰: "정답 hwp 와 비교해보세요"
  - DocInfo Tag 분포 비교: Tag 24 (BULLET) ORACLE 4 vs rhwp 0
  - HWPX `<hh:bullets itemCnt="4">` 의 `<hh:bullet>` 4개 (char ❏/※/­/❍) 파싱 누락
  - 정정: `src/parser/hwpx/header.rs` — `bullet` Empty event 분기 + 신규 `parse_bullet_hwpx` 헬퍼
  - 작업지시자 한컴 시각 판정 통과: "성공입니다"
- [x] **Stage 15 회귀 가드 + sweep + WASM**:
  - `tests/issue_1058_textbox_list_header.rs` 5 신규 회귀 가드 추가 (9/9 통과):
    * paragraph_instance_id_mapped / para_shape_line_spacing_v2_synced
    * style_lang_id_preserved / bullet_records_preserved / serialized_bullet_count
  - CI 패턴: cargo test --release --lib **1323 passed** + tests FAILED 0 + clippy + fmt clean
  - 광범위 sweep 12 fixtures × 229 SVG **diff=4** (footnote-01-hwpx 의도된 본질 정정, HWP 출처 11 fixture 회귀 부재)
  - WASM Docker 빌드 **4.91 MB** + rhwp-studio/public 동기화
- [x] Stage 보고서 `mydocs/working/task_m100_1058_stage15.md`
- [x] no-ff merge `f812abcf` → devel push (`f812abcf`)
- [x] 이슈 #1058 추가 comment (재정정 영역 상세)
- [x] **Round 5 (Stage 16): 신규 각주 본문 입력 contract 정합**:
  - 작업지시자 보고 (saved/222footnote-01.hwp, saved/111footnote-01.hwp): "한컴에디터에서 각주영역에 2)가 안보임"
  - 정답지 vs 저장본 inner_para diff: char_offsets=[0,1,2,...] (AutoNumber 8 cu jump 누락) vs ORACLE [0, 8, 9, 10, ...]
  - 정정 (3 영역):
    * `src/document_core/commands/object_ops.rs::insert_footnote_native` — inner_para text="  " (placeholder ×2) + char_offsets=[0, 8] + char_count=10
    * `src/model/paragraph.rs` — insert_text_at + control_text_positions 의 인라인 컨트롤 분기에 Control::AutoNumber 추가
    * `rhwp-studio/src/engine/cursor.ts::enterFootnoteMode` — caret 초기 위치 _fnCharOffset = 2 (placeholder 뒤)
  - 회귀 가드 1 신규 (`issue_1058_new_footnote_inner_para_contract`) + 9 기존 = 10/10 통과
  - 자동 reproduce: `examples/repro_1058_footnote_insert.rs` → `output/poc/issue_1058/repro_round5.hwp`
  - 재파싱 정합: text="  기술이란?" char_offsets=[0, 8, 9, 10, 11, 12, 13] char_count=15 (정답지 패턴 정확)
  - CI 패턴: cargo test --release --lib **1323 passed** + tests 0 failed + clippy + fmt clean
  - WASM Docker 빌드 4.91 MB + rhwp-studio/public 동기화
  - 작업지시자 한컴 한글 2020 시각 판정 통과: "동작 테스트 통과입니다"
- [x] Stage 보고서 `mydocs/working/task_m100_1058_stage16.md`
- [x] 트러블슈팅 문서 작성 `mydocs/troubleshootings/hwpx_footnote_save_5round_oracle_method.md` — 5 라운드 oracle 방법론 + 핵심 학습 5 (rhwp 자기 정합 ≠ 한컴 호환, 정답지 oracle 결정성, attr1 강제 set 위험, inline 컨트롤 8 cu 시스템 영향, 시각 판정 라운드 누적성) — 작업지시자 지시

핵심 — **작업지시자 Stage 4-pivot 통찰의 결정성 재입증**: Task #1050 의
"HWP → IR → HWP 라운드트립이 한컴 호환을 이미 보장하므로 HWPX → IR 만
HWP → IR 과 동일한 IR 을 생성하면 자동으로 한컴 호환" 접근이 본 task
reopen 의 4 라운드에서도 정확한 본질 식별 방법론. 매 라운드:

1. 작업지시자 한컴 시각 판정 보고
2. 정답지 vs 저장본 record-level diff 분석
3. 본질 영역 식별 + 정정
4. 시각 판정 재요청 + 통과

`feedback_visual_judgment_authority` 모범 사례 — 4 라운드 시각 판정 게이트
+ 매 라운드 정확한 본질 정정. rhwp 자기 정합 + 한컴 호환 모두 통과 입증.

`feedback_diagnosis_layer_attribution` 정합 — 정답지 vs 저장본 raw byte
정밀 비교로 본질 영역 정확 식별. ParaShape attr1 bit 7 강제 set 시도가
정답지 ps[5]=0 회귀 (글머리표 부작용) 유발 — 즉시 제거 + 정정 영역 한정.

**hwpx2hwp-rule.md contract unit 추가 (4개)** — Task #1050/#1052/#1058
누적 contract 의 일부:
- **PARA_HEADER instance_id** (HWPX `<hp:p id>` → UINT32 LE 6..10 offset)
- **Style record lang_id INT16 + trailing UINT16 zero** (HWP5 spec 표 47, hwplib 정합)
- **ParaShape line_spacing_v2 UINT32** (5.0.2.5 이상)
- **HWPX `<hh:bullet>` → HWP BULLET record** (한컴 default 글머리표 부재 시 자동 부여 결함 정정)

`project_hwpx_to_hwp_adapter_limit` 정합 + **단순 어댑터 한계 점진 돌파**.
Task #178 의 광범위 한컴 거부 영역의 일부 (각주 + 글상자 + 본 task 의
4 record contract) 를 HWP IR oracle 방식으로 정합.

후속:
- Tag 30 (FORBIDDEN_CHAR) + 31 (COMPATIBLE_DOCUMENT) + 94 (LAYOUT_COMPATIBILITY) 영역 (별개)
- 광범위 HWPX → HWP 호환 (Task #178 영역) — 별도 task

보고서: `mydocs/working/task_m100_1058_stage15.md` + 기존 `mydocs/report/task_m100_1058_report.md`
