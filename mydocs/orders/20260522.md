# 2026-05-22 — 오늘 할일

## Task M100-1058 reopen — 한컴 contract 4 영역 추가 정정 (closes #1058, 메인테이너)

- [x] 작업지시자 한컴 시각 판정 부적합 보고 (Stage 4 머지 후) — 글상자 없는 fixture (footnote-01.hwpx) 도 동일 다단계 목록 부작용 발생
- [x] 작업지시자 결정적 정보 수용:
  - 글상자 없는 케이스도 발생 → 글상자 LIST_HEADER 13 byte (Task #1058 1차) 가 본질 아님
  - HWP→HWP 라운드트립 정상 → HWPX→HWP 만의 본질
- [x] Stage 4-pivot "HWP IR oracle 방식" 재적용 — HWP 정답지 record vs HWPX→HWP 저장본 record-level 차이 분석
- [x] **Round 1 (Stage 5~9): PARA_HEADER instance_id**:
  - footnote-01.hwp 의 모든 PARA_HEADER (58개) 의 마지막 byte 0x80 vs rhwp 0x00 차이
  - HWPX `<hp:p id="2147483648">` (= 0x80000000) 매핑 누락
  - 정정: `src/parser/hwpx/section.rs::parse_paragraph` — raw_header_extra offset 6..10 작성
  - 작업지시자 한컴 시각 판정 통과: "문단번호 1.1.1.1.1.1 부여 사라짐"
- [x] **Round 2 (Stage 10~12): Style record lang_id**:
  - 작업지시자 보고: "왼쪽 여백 60.0pt + 줄간격 160% 자동 설정"
  - 작업지시자 통찰: "스타일 바인딩이 잘못된 것은 아닌가 확인 필요"
  - HWP5 spec 표 47 정합 분석 — Style record 의 INT16 lang_id + trailing UINT16 zero 4 byte 누락
  - 정정 (4 파일):
    * `src/model/style.rs` — Style.lang_id 필드 추가
    * `src/parser/doc_info.rs::parse_style` — lang_id 읽기 + trailing 흡수
    * `src/serializer/doc_info.rs::serialize_style` — lang_id + trailing zero 작성
    * `src/parser/hwpx/header.rs::parse_style` — HWPX langID 매핑
  - 추가: `src/parser/hwpx/header.rs::parse_para_shape` — line_spacing_v2 보정
  - 작업지시자 한컴 시각 판정 통과: "각주 삽입 시 정상 동작"
- [x] **Round 3 (Stage 13): ParaShape attr1 부작용 정정**:
  - 작업지시자 보고: "일반 문단 시작에 글머리표 처리"
  - 정답지 vs 저장본 attr1 비교 — ps[5] ORACLE 0x00000000 vs rhwp 0x00000080 (강제 set)
  - `ps.attr1 |= 0x80` (bit 7) 강제 set 제거 → HWPX 의 attr1 값 그대로 보존
  - 본질 미해결 (잔존 차이 발견 필요)
- [x] **Round 4 (Stage 14): HWPX `<hh:bullet>` 파싱**:
  - 작업지시자 통찰: "정답 hwp 와 비교해보세요"
  - DocInfo Tag 분포 비교: Tag 24 (BULLET) ORACLE 4 vs rhwp 0
  - HWPX `<hh:bullets itemCnt="4">` 의 `<hh:bullet>` 4개 (char ❏/※/­/❍) 파싱 누락
  - 정정: `src/parser/hwpx/header.rs` — `bullet` Empty event 분기 + 신규 `parse_bullet_hwpx` 헬퍼
  - 작업지시자 한컴 시각 판정 통과: "성공입니다"
- [x] **Stage 15 회귀 가드 + sweep + WASM**:
  - `tests/issue_1058_textbox_list_header.rs` 5 신규 회귀 가드 추가 (9/9 통과):
    * paragraph_instance_id_mapped / para_shape_line_spacing_v2_synced
    * style_lang_id_preserved / bullet_records_preserved / serialized_bullet_count
  - CI 패턴: cargo test --release --lib **1323 passed** + tests FAILED 0 + clippy + fmt clean
  - 광범위 sweep 12 fixtures × 229 SVG **diff=4** (footnote-01-hwpx 의도된 본질 정정, HWP 출처 11 fixture 회귀 부재)
  - WASM Docker 빌드 **4.91 MB** + rhwp-studio/public 동기화
- [x] Stage 보고서 `mydocs/working/task_m100_1058_stage15.md`
- [x] no-ff merge `f812abcf` → devel push (`f812abcf`)
- [x] 이슈 #1058 추가 comment (재정정 영역 상세)
- [x] **Round 5 (Stage 16): 신규 각주 본문 입력 contract 정합**:
  - 작업지시자 보고 (saved/222footnote-01.hwp, saved/111footnote-01.hwp): "한컴에디터에서 각주영역에 2)가 안보임"
  - 정답지 vs 저장본 inner_para diff: char_offsets=[0,1,2,...] (AutoNumber 8 cu jump 누락) vs ORACLE [0, 8, 9, 10, ...]
  - 정정 (3 영역):
    * `src/document_core/commands/object_ops.rs::insert_footnote_native` — inner_para text="  " (placeholder ×2) + char_offsets=[0, 8] + char_count=10
    * `src/model/paragraph.rs` — insert_text_at + control_text_positions 의 인라인 컨트롤 분기에 Control::AutoNumber 추가
    * `rhwp-studio/src/engine/cursor.ts::enterFootnoteMode` — caret 초기 위치 _fnCharOffset = 2 (placeholder 뒤)
  - 회귀 가드 1 신규 (`issue_1058_new_footnote_inner_para_contract`) + 9 기존 = 10/10 통과
  - 자동 reproduce: `examples/repro_1058_footnote_insert.rs` → `output/poc/issue_1058/repro_round5.hwp`
  - 재파싱 정합: text="  기술이란?" char_offsets=[0, 8, 9, 10, 11, 12, 13] char_count=15 (정답지 패턴 정확)
  - CI 패턴: cargo test --release --lib **1323 passed** + tests 0 failed + clippy + fmt clean
  - WASM Docker 빌드 4.91 MB + rhwp-studio/public 동기화
  - 작업지시자 한컴 한글 2020 시각 판정 통과: "동작 테스트 통과입니다"
- [x] Stage 보고서 `mydocs/working/task_m100_1058_stage16.md`
- [x] 트러블슈팅 문서 작성 `mydocs/troubleshootings/hwpx_footnote_save_5round_oracle_method.md` — 5 라운드 oracle 방법론 + 핵심 학습 5 (rhwp 자기 정합 ≠ 한컴 호환, 정답지 oracle 결정성, attr1 강제 set 위험, inline 컨트롤 8 cu 시스템 영향, 시각 판정 라운드 누적성) — 작업지시자 지시

핵심 — **작업지시자 Stage 4-pivot 통찰의 결정성 재입증**: Task #1050 의
"HWP → IR → HWP 라운드트립이 한컴 호환을 이미 보장하므로 HWPX → IR 만
HWP → IR 과 동일한 IR 을 생성하면 자동으로 한컴 호환" 접근이 본 task
reopen 의 4 라운드에서도 정확한 본질 식별 방법론. 매 라운드:

1. 작업지시자 한컴 시각 판정 보고
2. 정답지 vs 저장본 record-level diff 분석
3. 본질 영역 식별 + 정정
4. 시각 판정 재요청 + 통과

`feedback_visual_judgment_authority` 모범 사례 — 4 라운드 시각 판정 게이트
+ 매 라운드 정확한 본질 정정. rhwp 자기 정합 + 한컴 호환 모두 통과 입증.

`feedback_diagnosis_layer_attribution` 정합 — 정답지 vs 저장본 raw byte
정밀 비교로 본질 영역 정확 식별. ParaShape attr1 bit 7 강제 set 시도가
정답지 ps[5]=0 회귀 (글머리표 부작용) 유발 — 즉시 제거 + 정정 영역 한정.

**hwpx2hwp-rule.md contract unit 추가 (4개)** — Task #1050/#1052/#1058
누적 contract 의 일부:
- **PARA_HEADER instance_id** (HWPX `<hp:p id>` → UINT32 LE 6..10 offset)
- **Style record lang_id INT16 + trailing UINT16 zero** (HWP5 spec 표 47, hwplib 정합)
- **ParaShape line_spacing_v2 UINT32** (5.0.2.5 이상)
- **HWPX `<hh:bullet>` → HWP BULLET record** (한컴 default 글머리표 부재 시 자동 부여 결함 정정)

`project_hwpx_to_hwp_adapter_limit` 정합 + **단순 어댑터 한계 점진 돌파**.
Task #178 의 광범위 한컴 거부 영역의 일부 (각주 + 글상자 + 본 task 의
4 record contract) 를 HWP IR oracle 방식으로 정합.

후속:
- Tag 30 (FORBIDDEN_CHAR) + 31 (COMPATIBLE_DOCUMENT) + 94 (LAYOUT_COMPATIBILITY) 영역 (별개)
- 광범위 HWPX → HWP 호환 (Task #178 영역) — 별도 task

보고서: `mydocs/working/task_m100_1058_stage15.md` + 기존 `mydocs/report/task_m100_1058_report.md`

## Task M100-1061 — [hwpx2hwp] 수식과 글자 조합된 문서 저장 구현 (#1061)

- [x] 작업지시자 자료 수용:
  - `samples/hwpx/math-001.hwpx` (HWPX 원본, 수식 2개 포함)
  - `samples/math-001.hwp` (정답지 — 한컴 직접 저장)
  - `pdf-large/hwpx/math-001.pdf` (시각 정답지)
  - `saved/111math-001.hwp` (현재 rhwp-studio 저장 결과 — 본 task 출발점)
- [x] `local/task1061` 브랜치 생성
- [x] 사전 조사 (Explore agent):
  - HWPX 수식 파싱 정상 (parser/hwpx/section.rs::parse_equation)
  - HWP5 수식 파싱/직렬화 정상 (parser/control.rs::parse_equation_control + serializer/control.rs::serialize_equation_control)
  - Equation IR struct (model/control.rs:64-82) — script + font_size + color + baseline + font_name + version_info + raw_ctrl_data
  - **누락 영역 후보**: document_core/converters/hwpx_to_hwp.rs 의 Equation case 미처리 (확인 필요)
- [x] 수행 계획서 `mydocs/plans/task_m100_1061.md` → 작업지시자 승인
- [x] 구현 계획서 `mydocs/plans/task_m100_1061_impl.md` (4 단계) → 작업지시자 승인
- [x] **Stage 1: HWPX 어댑터 Equation arm + GenShape attr bit 27 보강**
  - 정답지 vs 저장본 IR diff: common.attr 0x0C2A2211 vs 0x042A2211 (bit 27 누락)
  - 정정: `src/document_core/converters/hwpx_to_hwp.rs` — AdapterReport 신규 필드 + adapt_paragraph match arm + 신규 adapt_equation 함수 (pack_common_attr_bits | 0x08000000 + raw_ctrl_data clear)
  - 작업지시자 시각 판정 보고: "수식만 보이고 문자들이 안 보임" → Stage 2 필요
  - Stage 보고서 `mydocs/working/task_m100_1061_stage1.md`
- [x] **Stage 2: EQEDIT unknown UINT2 field + HWPX font 복원**
  - 본질 식별 (Stage 4-pivot): EQEDIT raw payload 직접 분석으로 HWP5 spec 표 105 errata 발견
  - baseline 과 version_info 사이 UINT16 zero 누락 (hwplib `ForEQEdit.readUInt2()` 정합)
  - 누락 시 (version_info, font_name) 자리값 swap → 후속 record (PARA_TEXT) byte align 손상 → 본문 미표시
  - 정정 (5 영역): src/model/control.rs::Equation `unknown: u16` 신규, parser/control.rs `read_u16()`, serializer/control.rs `write_u16`, parser/hwpx/section.rs `unknown: 0`, serializer/hwpx/mod.rs test 갱신
  - 작업지시자 시각 판정 통과: "output/poc/issue_1061/repro_stage1.hwp 도 본문이 잘 보입니다"
- [x] **Stage 3: 회귀 가드 7/7 통과 + CI 패턴**
  - tests/issue_1061_equation_serialize.rs 신규 7 가드 (attr bit27 / first_attr / unknown_field / saved_font_version / count / script / paragraph char_offsets)
  - cargo test --release --lib **1323 passed** + tests 0 failed + clippy + fmt clean
- [x] **Stage 4: WASM + 보고서 + 트러블슈팅**
  - WASM Docker 빌드 4.91 MB + rhwp-studio/public 동기화
  - 작업지시자 rhwp-studio 동작 판정 통과
  - 트러블슈팅 `mydocs/troubleshootings/hwpx_equation_save_eqedit_spec_errata.md`
  - spec errata 갱신 `mydocs/tech/hwp_spec_errata.md` — EQEDIT unknown UINT2 항목 추가
  - 최종 보고서 `mydocs/report/task_m100_1061_report.md`

핵심 — **HWP5 spec 표 105 의 errata 발견**: EQEDIT 의 baseline 과 version_info 사이
UINT16 zero (2 byte) 가 spec 에 누락. hwplib `ForEQEdit.readUInt2()` 가 정확 처리.
누락 시 (version_info, font_name) 자리값 swap → 후속 record byte align 손상 →
한컴 본문 미표시. Stage 4-pivot oracle 방법론 (Task #1058) 의 raw byte 분석이 본질
식별의 결정적 도구. `reference_hwp2hwpx_library` (hwplib) 가 spec errata 발견의
권위 근거.

`feedback_self_verification_not_hancom` 정합 — Stage 1 후 rhwp 정상 / 한컴 본문
미표시 단계적 회복 (Stage 2 후 완전 호환). 자기 검증 한계의 결정적 재입증.

`feedback_diagnosis_layer_attribution` 정합 — IR 차원 정합 ≠ raw byte 정합.
IR diff 만으로는 본질 미달, raw byte 직접 분석 필수.

**hwpx2hwp-rule.md contract unit 추가 (Task #1050/#1052/#1058 누적)**:
- **HWPX `<hp:equation>` → HWP CTRL_HEADER attr bit 27 (0x08000000)** (Table 패턴 정합)
- **HWP5 EQEDIT spec 표 105 errata** — baseline 과 version_info 사이 UINT2 zero
  (hwplib ForEQEdit.readUInt2 정합)

## Task M100-1064 — [hwpx2hwp] el-school-001.hwpx 저장 (#1064)

- [x] 작업지시자 자료 수용:
  - `samples/hwpx/el-school-001.hwpx` (HWPX 원본 — 셀 안 그림+도형 포함 표 2개)
  - `samples/el-school-001.hwp` (정답지)
  - `pdf-large/hwpx/el-school-001.pdf` (시각 정답지)
  - `saved/111el-school-001.hwp` (현재 rhwp-studio 저장 결과 — 본 task 출발점)
- [x] 작업지시자 보고 3 증상:
  1. 한컴 에디터 로딩 중 종료 (파일 손상)
  2. rhwp-studio 첫 표 0번 셀 이미지 비율 미처리
  3. rhwp-studio 첫 표 0번 셀 도형 출력 안 됨
- [x] 트러블슈팅 사전 검색 — 직접 관련 없음
- [x] 이슈 #1064 등록 (assignee 본인, hwpx2hwp/bug label, v1.0.0 milestone)
- [x] `local/task1064` 브랜치 생성
- [x] 사전 조사 (정답지 vs 저장본 record-level diff):
  - tag 87 (HWPTAG_CTRL_DATA) — **정답지 2 vs 저장본 0** (누락 본질 후보)
  - 위치: 표 control (`' lbt'` = 'tbl ') 직후 level=2 (HWP5 spec 표 표 CTRL_HEADER 자식 record)
  - payload 104 byte 동일 — ParameterSet 구조 (`1B 02 01 00 00 00 42 02 ...`)
  - 두 표 (rec_idx 48, 105) 모두 동일 payload — 셀 안 도형 포함 표의 메타데이터
- [x] 수행 계획서 `mydocs/plans/task_m100_1064.md` → 승인
- [x] 구현 계획서 `mydocs/plans/task_m100_1064_impl.md` (4 단계) → 승인
- [x] **Stage 1: 진단 도구 + ParameterSet 정밀 분석**
  - `examples/dump_table_ctrl_data.rs` 신규
  - ParameterSet 구조 식별 (id 0x021B / 자식 0x0242 / 11 Integer4)
  - 11 parameters 의미 추정 (vertical/horizontal_offset, width/height, row_height, flag×4, page_width/height)
  - hwplib `ForParameterSet.java` 권위 정합
  - Stage 보고서 `mydocs/working/task_m100_1064_stage1.md`
- [x] **Stage 2 시도: 어댑터 표 CTRL_DATA 동적 합성 (셀 안 도형 조건)**
  - `src/document_core/converters/hwpx_to_hwp.rs` 에 `adapt_table_ctrl_data_in_paragraph` 신규
  - 결과: repro_stage2.hwp 의 CTRL_DATA 2 records (size 104) 정합
  - 작업지시자 한컴 시각 판정 보고: "한컴 여전히 로딩 실패"
  - 추가 record-level diff 분석:
    * **SHAPE_COMPONENT (tag 82) size 정답지 40 vs 저장본 4** — 36 byte 누락 ×4 (셀 안 도형 본체)
    * PARA_HEADER size 24 vs 22 — 2 byte 누락
  - 작업지시자 추가 정보: "hwpx 처음 rhwp-studio 로 열때부터 셀안 도형 미출력 + hwp 도형 방향 잘못"
- [x] **Stage 2 롤백 + 본 task 종결 결정** (작업지시자 결정)
  - 진짜 본질이 HWPX 파서 의 셀 안 도형 처리 누락 — Stage 2 의 CTRL_DATA 합성으로 부족
  - `git checkout src/document_core/converters/hwpx_to_hwp.rs` 롤백
  - 본 task 는 조사 결과 + 진단 도구 보존
  - 최종 보고서 `mydocs/report/task_m100_1064_report.md`
- [x] **별도 task 분리 등록** (신규 fixture):
  - 신규 자료: `samples/hwpx/shape-001.hwpx` + `samples/shape-001.hwp` + `pdf-large/hwpx/shape-001.pdf`
  - 증상 명확화 (작업지시자): "hwp 파일은 첫 번째 도형이 180도 회전된 상태로 출력되며, hwpx 는 출력되지 않습니다"
  - 신규 이슈 #1067 등록 + 본 task #1064 와 분리

## Task M100-1067 — HWPX 도형 IR 화 + HWP/HWPX 회전 부호 정정 (#1067)

- [x] 이슈 #1067 등록 + assignee 본인
- [x] 사전 조사 (Explore agent): HWPX 파서 정합 + rhwp-studio LayerPathOp.transform 누락 식별
- [x] 수행 계획서 `mydocs/plans/task_m100_1067.md` → 승인
- [x] 구현 계획서 `mydocs/plans/task_m100_1067_impl.md` → 승인
- [x] **Stage 1** (canvaskit LayerPathOp.transform): rhwp-studio/src/core/types.ts + canvaskit-renderer.ts 정정
  - 작업지시자 보고: "변화 없음" (canvas2d backend 사용 중 — 보완적 정정)
- [x] **Stage 2** (다각형 dump 통한 본질 식별):
  - HWPX `<hc:pt>` 점 파싱 누락 → PolygonShape::points 빈 Vec → HWPX 도형 미표시
  - 정정: src/parser/hwpx/section.rs::parse_shape_object 에 b"pt" 분기 추가
  - HWPX IR: points 0→4 (정답지 정합)
- [x] **Stage 3** (SVG 회전 부호 + U+FFFC 정정):
  - 작업지시자 보고: "첫 다각형이 한컴과 180도 반시계 방향"
  - 정정: flip + 회전 동시 적용 시 회전 부호 반전 (src/renderer/svg.rs + web_canvas.rs)
  - 작업지시자 추가 질문: "OBJ 출력 이유?" → U+FFFC OBJECT REPLACEMENT CHARACTER (inline 컨트롤 placeholder)
  - 정정: draw_text 가 U+FFFC skip (svg + web_canvas)
  - 작업지시자 시각 판정 통과: "정상 — 정답지 이미지 정합"
- [x] WASM Docker 빌드 4.91 MB + rhwp-studio/public 동기화
- [x] **잔존 (1)(2) 별도 task 분리 결정** (작업지시자):
  - (1) HWPX TAC 도형 paragraph 가로 위치 (HWP/HWPX IR 동일이지만 SVG x ~6px 차이)
  - (2) 한글자처럼 캐럿 이동 (rhwp-studio cursor + Rust char_offsets)
  - 이슈 #1071 등록 (병합)
- [x] 회귀 가드 `tests/issue_1067_shape_rotation.rs` 5/5 통과
- [x] CI: lib 1324 + tests 0 failed + clippy + fmt clean
- [x] 최종 보고서 `mydocs/report/task_m100_1067_report.md`
