# 2026-05-17 — 오늘 할일

## Task #952 — hwp3-sample16 외곽선 밖 내용 돌출 (PR #956)

본질 분석 결과 3개 별개 회귀로 분리. Issue 1 만 본 task 에서 해결, Issues 2/3 별도 task 분리.

- [x] 본질 진단 — 3개 별개 회귀로 분리 확정
- [x] Issue 1 (페이지 외곽선 paper/body) — `paper_based = true` fix + RHWP_DEBUG_PAGE_BORDER (PR #956)
- [x] Issue 1 시각 검증 (sample16 HWP3/HWP5/HWPX + 시험지 5+ samples 한컴 정합)
- [x] Issue 2 root cause 분석 + 별도 task #957 분리
- [x] Issue 3 root cause 분석 + 별도 task 분리 (시험지 page 1 문9 vertical)

## Task #957 — sample16 page 18 본문 다음 페이지 밀림 (원 #952 Issue 2, PR #958)

- [x] Issue 등록 + branch + 수행/구현 계획서 + 승인
- [x] Stage 1 — Cursor over-advance source 식별 (RHWP_DEBUG_TAC_CURSOR)
  - Shape pi=394 ci=1 (picture diagram) 이 +430.6px advance 확정
  - 빈 caption (text="") 의 phantom advance + 잘못된 pic_y (has_prior_tac 갱신값) 의 복합 결함
- [x] Stage 2 — 구현 계획 V2 (Fix A 선정)
- [x] Stage 3 — Fix A 적용 + 단위 검증 (sample16 page 18 본문 같은 페이지 표시)
- [x] Stage 4 — 회귀 검증 (cargo test 1288 + sample14 caption text 보유 sample 정상)
- [x] Stage 5 — 시각 검증 + 최종 보고서

## 후속

- Issue 3 별도 task 등록 — HWP5 시험지 page 1 문9 vertical 처짐 (원 #952 Issue 3)

---

## M100 — PR 처리 (메인테이너, 5/17~18 사이클)

| PR | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| **PR #956 (closes #952 Issue 1)** | fix — 쪽 테두리 paper-based outline 강제 (#920 비트 해석 회귀 정정) | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `b31e38ff`, 작업지시자 시각 판정 통과)** | 컨트리뷰터 @jangster77 (Taesup Jang) — **24+ 사이클** 핵심 컨트리뷰터 (HWP3/WMF/page border) — **연속 5 PR 1번째** (#956 → #958 → #961 → #963 → #964). **사전 작업 — 원격 동기화** (작업지시자 지시): 본 세션 5/11 작업 후 정체, origin/devel 영역 영역 5/12~17 PR #919~#954 누적 → 본 세션 5/11 작업 (PR #786~#818 + v0.7.11) origin 반영 확인 + devel 3자 동기화 (`local/devel`=`devel`=`origin/devel`=`016e694c`, +19 commits FF) + WASM 4.4 MB 빌드. `feedback_release_sync_check` 권위 사례 강화. 본질: 페이지 외곽선 paper/body 잘못 분류 회귀 — 회귀 source `4bb11289` (#920) `paper_based = (attr & 0x01) == 0` 비트 반전 (bisect: task877 baseline paper-based ✓ → #920 body-based ⚠️ → 현재 devel layout.rs:764 회귀 잔존). 진단: 5+ samples 한컴 viewer 실측 — attr 0/1, textBorder PAPER/CONTENT 양쪽 다 paper-based, bit 0 은 outline 위치 결정 비트 아님. 회귀 history: task877 (`!= 0`) → #920 (`== 0`) → 본 PR (`true` 모든 sample 정합). 정정 (`src/renderer/layout.rs` +16/-2): `paper_based = true` 강제 + `RHWP_DEBUG_PAGE_BORDER` 진단 영구화 + 회귀 history 코멘트 영구 보존. 추가 (13 files): 시험지 fixture 8 (HWP/HWPX) + 한컴 2022 권위 PDF 4 + 문서 4. 본 PR 범위 외 (별도 task 분리, PR 본문 명시): Issue 2 (sample16 page 18 본문 밀림, typeset multi-TAC cursor over-advance) + Issue 3 (시험지 문9 vertical 처짐, HWP5 column) — 부분 해결 + 명확한 분리 (archive/task936 "9회 시도 + 5회 revert" 대조 교훈). 본 환경 cherry-pick `bb3f40ad` 충돌 0건 (CLEAN). 결정적 검증: cargo test --release --lib **1288 passed, 0 failed** + cargo clippy --release -D warnings 통과 + **광범위 sweep 7 fixture / 169 페이지 / 169 same / 0 diff** (기존 fixture 회귀 부재, sweep fixture 영역 영역 sample16/시험지 미포함 → 작업지시자 시각 검증 영역 영역 핵심 게이트) + WASM 4.4 MB 재빌드. **작업지시자 시각 판정** ✅ 통과 (sample16 HWP3/HWP5/HWPX page 17 + 시험지 3-09월/3-11월 page 1 외곽선 paper-based, 한컴 2022 PDF 권위). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff. Issue #952 OPEN 유지 (Issue 2/3 별도 task 분리). 검토 보고서: `mydocs/pr/archives/pr_956_review.md`. 처리 보고서: `mydocs/pr/archives/pr_956_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 24+ 사이클. **`feedback_release_sync_check` 권위 사례 강화** — PR 작업 전 원격 동기화 선행 (5/11→5/17 +19 commits FF). **`feedback_image_renderer_paths_separate` 정합** — layout.rs page border 분기 단일. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화** — bit 0 추측 해석 (task877/#920) → 5+ sample 한컴 실측 영역 영역 `true` 강제 (추측보다 실측). **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — 회귀 source `4bb11289` (#920) bisect 정확 + Issue 1/2/3 분리 진단. **`feedback_pr_supersede_chain` 정합** — task877 → #920 (회귀 도입) → **#956** (회귀 정정) (c) 패턴. **`reference_authoritative_hancom` 정합** — 시험지 한컴 2022 PDF 권위 영구 보존 + 회귀 fixture. **`feedback_v076_regression_origin` 정합** — #920 추측 bit 해석 → 작업지시자 환경 회귀 → 한컴 실측 영역 영역 정정 패턴. |
